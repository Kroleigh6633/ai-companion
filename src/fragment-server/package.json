{
  "name": "@ai-companion/fragment-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test src/**/*.node.test.ts",
    "test:coverage": "node --experimental-strip-types --experimental-test-coverage --test src/**/*.node.test.ts"
  },
  "dependencies": {
    "@ai-companion/types": "workspace:*",
    "@ai-companion/utils": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "esbuild": "^0.25.0",
    "vitest": "^3.0.0",
    "@vitest/coverage-v8": "^3.0.0",
    "@types/node": "^22.0.0",
    "@types/uuid": "^10.0.0"
  }
}
